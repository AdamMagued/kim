# Copy to `.env.development.local` (gitignored).
#
# Dev defaults to the design-mock shell (`FullWindowShell`) so `dm-*` styles are visible.
# Set this when you want the real Kim app during development:
# VITE_KIM_REAL_UI=true
