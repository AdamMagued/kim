# ── Version pinning (#55) ────────────────────────────────────────────────────
# Dependencies are pinned with compatible-release (~=) to prevent unexpected
# major-version breakage while still receiving patch-level security fixes.
# For fully reproducible deploys, commit a generated lockfile:
#   pip install -r requirements.txt && pip freeze > requirements-lock.txt
# ── Core ─────────────────────────────────────────────────────────────────────
mcp~=1.0
anthropic~=0.40
openai~=1.50
python-dotenv~=1.0
PyYAML~=6.0
httpx~=0.27
aiofiles~=23.0
aiohttp~=3.9
aiosqlite~=0.20
json5~=0.10
json-repair~=0.30
pyperclip~=1.8

# ── Screen / Input Automation ────────────────────────────────────────────────
pyautogui~=0.9.54
mss~=9.0
Pillow~=10.0
playwright~=1.40

# ── Relay Server ─────────────────────────────────────────────────────────────
# fastapi and uvicorn are NOT listed here — they are relay-only dependencies.
# Install them via:  pip install -r requirements-relay.txt

# ── Window Management (platform-conditional) ────────────────────────────────
# pygetwindow: Windows only — used for window listing, focus, and resize.
#   On macOS, osascript (built-in) is used instead. No extra dependency.
#   On Linux, install wmctrl and/or xdotool system packages instead.
pygetwindow~=0.0.9; sys_platform == "win32"

# ── macOS-only dependencies ─────────────────────────────────────────────────
# pyobjc-core and pyobjc-framework-Quartz are optional — they improve
# screenshot quality and enable some advanced window queries on macOS.
# osascript (used for window management) is built-in and needs no pip dep.
pyobjc-core~=10.0; sys_platform == "darwin"
pyobjc-framework-Quartz~=10.0; sys_platform == "darwin"

# ── Input Automation ────────────────────────────────────────────────────────
pynput~=1.7

# ── Testing ──────────────────────────────────────────────────────────────────
pytest~=8.0
pytest-asyncio~=0.23

# ── Voice / TTS (optional — install separately for Kim's voice) ─────────────
# kokoro, sounddevice, and soundfile are NOT listed here — they are optional.
# Install them via:  pip install -r requirements-voice.txt
