# Minimal dependencies for the relay server only.
# Version pinning (#56): compatible-release pins (~=) prevent major-version
# breakage. For reproducible Docker builds, generate a pinned lockfile:
#   pip install -r requirements-relay.txt && pip freeze > requirements-relay-lock.txt
# Use the lockfile for the Docker image so it stays small (~50 MB).
# The full requirements.txt is for the local PC agent.
fastapi~=0.115
uvicorn[standard]~=0.30
pydantic~=2.0
aiosqlite~=0.20
python-dotenv~=1.0
